node_modules
dist
.git
.github
.claude
*.log
# Personal data — provided at runtime via env + a mounted /data volume
gangs.yaml
policies.yaml
devices.yaml
config/config.json
# Personal data — never send to the build context
codes.yaml
mirrors.yaml
